var revMenuItemWidget = (revVarArgs) => {

    return `
            <div class="revIMHeaderTab">
                <i class="fas fa-tty revIMHeaderTabIcon"></i>
            </div>
        `;
}

module.exports.revMenuItemWidget = revMenuItemWidget;